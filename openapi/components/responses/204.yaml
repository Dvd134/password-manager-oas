description: Unauthorized
content:
  application/json:
    schema:
      $ref: "./MetaResponse.yaml"
    example:
      meta:
        status: succeeded
        messages:
        - code: No Content
          text: Account has been deleted
          level: info
            
description: Current browser tab`s URL not found
content:
  application/json:
    schema:
      $ref: "./MetaResponse.yaml"
    example:
      meta:
        status: failed
        messages:
        - code: ACTIVE_TAB_URL_NOT_FOUND
          level: error
          text: Current browser tab`s URL not found
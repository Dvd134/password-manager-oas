description: Something went wrong on server side
content:
  application/json:
    schema:
      $ref: "./MetaResponse.yaml"
    example:
      meta:
        status: failed
        messages:
        - code: Internal Server Error
          text: Server error
          level: error
          